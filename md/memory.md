# BỘ NHỚ CÁ NHÂN (MEMORY)

Tệp này đóng vai trò như một không gian ghi nhớ ngắn hạn và dài hạn cho AI hoặc Developers. Lưu lại các bối cảnh, lỗi phổ biến, các quyết định lớn để giữ tính liên tục.

## 1. Ngữ cảnh làm việc hiện tại (Current Context)
- Đã hoàn thiện toàn bộ hệ thống kiểm soát chất lượng code (Code Quality Pipeline) cho dự án.
- Bao gồm: Pre-commit Hook, CI/CD (GitHub Actions), AI Reviewer Agent, PR Template, Commitlint.
- Bước tiếp theo: Điền đầy đủ thông tin vào `CLAUDE.md` (Tên dự án, Tech Stack) và bắt đầu phát triển code thực tế theo ROADMAP Giai đoạn 1.
- **Module mới (2026-07-27): Dashboard Bảo trì đội xe (Fleet/BTBD)** — module nghiệp vụ thực tế đầu tiên, cắm vào khung sườn ở shell riêng `/fleet/*` (sidebar "FLEET OPS", tách khỏi nav chính bằng `FrameworkLayout`/`FleetLayout` trong `App.jsx`).
  - Nguồn dữ liệu: Google Sheet "M12 Data bot btbd 1" (owner nguyenduylinhauto@gmail.com), export snapshot lưu tại `backend/data/fleet_sheet_raw.md`.
  - **Tham chiếu thiết kế:** `dashboard/Fleet Dashboard.dc.html` — mockup ~20 màn hình dùng design system `dashboard/_ds/broadsheet-.../styles.css` (đã copy/scope lại thành `frontend/src/styles/fleet-theme.css` dưới class `.fleet-shell`) + icon Phosphor (CDN, load trong `index.html`). Mockup dùng dữ liệu bịa (driver, nhiên liệu, uptime, ảnh xe, AI...) — chỉ implement 9 màn có dữ liệu thật: Tổng quan, Tổng quan đội xe, Danh sách xe, Hồ sơ giấy tờ, Kế hoạch đăng kiểm, Tổng quan BD-SC, Nhật ký sửa chữa, Lịch bảo dưỡng, Tra cứu biển số. Các màn cần dữ liệu không có thật (Hiệu suất/nhiên liệu/uptime, Điểm sức khỏe xe, Hồ sơ ảnh xe, So sánh xe, Báo cáo, AI, Cài đặt) đã bỏ qua theo yêu cầu người dùng — nếu sau này có dữ liệu thật cho các phần này, có thể bổ sung tiếp theo đúng mockup.
  - Backend: `services/fleet_parser.py` (parser markdown-table), model `vehicle.py`/`maintenance_record.py`/`vehicle_maintenance_status.py`, `services/fleet_service.py`, `schemas/fleet.py`, `api/fleet.py` (mount `/api/v1/fleet`, endpoints: stats, records, alerts, vehicles, documents, maintenance-schedule, vehicle/{plate}). Script nạp dữ liệu: `scripts/seed_fleet_data.py`.
  - Frontend: toàn bộ nằm trong `frontend/src/fleet/` (tự chứa, dễ gỡ như 1 module) — `FleetLayout.jsx`, `theme.js`, `api.js`, `components/` (Donut, Bars, KpiCard, ProportionBar, Tag, DataTable — Donut/Bars port lại từ hàm vẽ SVG trong file mockup), `pages/` (9 trang trên).
  - **Cách cập nhật dữ liệu mới:** export lại Google Sheet → ghi đè `backend/data/fleet_sheet_raw.md` → chạy `python scripts/seed_fleet_data.py` (xoá và nạp lại toàn bộ 3 bảng, không cộng dồn).
  - Đã bỏ qua 1 bảng trùng lặp trong Sheet (bảng tổng hợp thứ 4, trùng dữ liệu với bảng theo dõi ODO thứ 3) và các cột chi tiết linh kiện ít dữ liệu (Turbo, Bố phanh, Máy phát...) để giữ scope gọn.
  - **Lưu ý:** môi trường code không có công cụ trình duyệt headless nên chưa tự chụp màn hình xác nhận UI được — mới verify qua `npm run build` (pass) + gọi trực tiếp API. Nên tự mở `http://localhost:3000/fleet` kiểm tra trực quan trước khi tin tưởng hoàn toàn.
  - **Cập nhật (cùng ngày) — mở rộng thêm các màn ban đầu bỏ qua**, theo yêu cầu người dùng, ưu tiên dữ liệu thật/suy ra minh bạch, không bịa số:
    - `Hồ sơ chi tiết xe` (`/fleet/xe/:plate`), `Điểm sức khỏe xe` (`/fleet/diem-suc-khoe`) — công thức điểm 100% từ dữ liệu thật (tuân thủ BD 40% + tần suất SC 25% + tình trạng BD 20% + hồ sơ giấy tờ 15%), khai báo rõ "không phải AI" trong UI.
    - `So sánh xe` (`/fleet/so-sanh`), `Báo cáo` (`/fleet/bao-cao` — theo tuần/tháng, có ghi chú rõ thiếu dữ liệu quãng đường/km theo kỳ).
    - `Cảnh báo tổng hợp` (`/fleet/canh-bao`) — rule-based (chi phí bất thường, sửa lặp lại, quá hạn nặng, nhiều giấy tờ hết hạn), ghi rõ không phải AI/ML.
    - `Sửa chữa dọc đường` (`/fleet/doc-duong`) — phát hiện bằng keyword match trên cột `note`/`detail` thật (không phải danh mục riêng trong Sheet).
    - `Lập phiếu bảo dưỡng` (`/fleet/lap-phieu`) — form thật, POST `/api/v1/fleet/records` tạo record mới trong DB.
    - `Cài đặt` (`/fleet/cai-dat`) — ngưỡng cảnh báo (hạn giấy tờ, km đến kỳ/sắp đến kỳ BD) đọc/ghi thật qua bảng `app_settings` (model mới), GET/PUT `/api/v1/fleet/settings`. Đã bỏ phần Người dùng/Phân quyền/Audit log của mockup vì chưa có module Auth — không bịa danh sách user giả.
    - Model mới: `models/app_settings.py`. Service mới trong `fleet_service.py`: `get_health_scores`, `get_smart_alerts`, `get_roadside_incidents`, `get_reports`, `create_maintenance_record`, `get_vehicle_compare`, `get_settings`/`update_settings`.
    - **Gotcha quan trọng:** `uvicorn --reload` (StatReload) trên máy Windows này hay bị treo ở "Reloading..." và không tự hồi phục sau khi sửa file — sau mỗi lần đổi code backend phải `Stop-Process` tiến trình `python` cũ rồi chạy lại `python main.py` sạch, không nên tin tưởng auto-reload khi kiểm thử.
  - **Cập nhật (2026-07-28) — khớp lại đúng thứ tự/tên sidebar theo ảnh chụp mockup gốc mà người dùng cung cấp:**
    - Đổi tên nhóm "Đội xe" → "Thông tin xe"; chuyển "Hồ sơ chi tiết xe" ra thành mục top-level (không còn nằm trong nhóm).
    - Nhóm "Bảo dưỡng · Sửa chữa" đổi tên các mục con để khớp mockup: "Lịch bảo dưỡng"→"Theo dõi bảo dưỡng" (route `/fleet/theo-doi-bao-duong`, vẫn là `MaintenanceSchedule.jsx`), "Nhật ký sửa chữa"→"Theo dõi sửa chữa" (route `/fleet/theo-doi-sua-chua`, vẫn là `RepairLog.jsx`), "Lập phiếu bảo dưỡng"→"Phiếu bảo dưỡng" (route `/fleet/phieu-bao-duong`).
    - Thêm mục top-level còn thiếu theo đúng thứ tự ảnh: **Hiệu suất đội xe** (`/fleet/hieu-suat`, `Performance.jsx` — chỉ số thật duy nhất tính được là giờ nằm xưởng từ `total_hours`, không có dữ liệu nhiên liệu/uptime nên không hiển thị 2 chỉ số đó), Điểm sức khỏe xe, Hồ sơ chi tiết xe, So sánh đội xe (route đổi thành `/fleet/so-sanh-doi-xe`), Tra cứu biển số, Báo cáo, **AI & Analytics** (route `/fleet/ai-analytics`, chính là trang cảnh báo rule-based cũ — đổi nhãn cho khớp mockup nhưng giữ nguyên disclaimer "không phải AI/ML" trong nội dung), Cài đặt.
    - Backend thêm `get_performance()` trong `fleet_service.py` + endpoint `GET /api/v1/fleet/performance`.
  - **Cập nhật (2026-07-28) — chuyển sang đồng bộ trực tiếp từ Google Sheet (không còn snapshot tĩnh):**
    - **Phát hiện quan trọng:** Sheet "M12 Data bot btbd 1" (id `1E_fYmNK3TIEMt3xz7JTwG5kmttz_lKbr8xtWRYTDQOY`) đang share **công khai dạng "Anyone with link can view"** — tải được toàn bộ workbook (mọi tab) qua `https://docs.google.com/spreadsheets/d/<id>/export?format=xlsx` mà **không cần service account/API key**. Đã xác nhận 4 tab thật: `Lịch sử Bảo dưỡng - sửa chữa` (nhật ký, 20 cột), `Data xe` (danh sách xe, 40 cột), `Data BTBD` (theo dõi ODO, 36 cột), `Thông tin bảo dưỡng` (bảng trùng lặp, vẫn bỏ qua). Cấu trúc cột giống hệt bản snapshot cũ nên tái dùng đúng index mapping của `fleet_parser.py`.
    - Data thật hiện tại (2026-07-28) lớn hơn nhiều snapshot ban đầu: **214 xe, 3205 nhật ký BTBD** (thay vì 59 xe/123 record) — sheet đã được cập nhật liên tục theo thời gian thực từ team vận hành.
    - Module mới `services/sheet_sync.py`: `fetch_workbook()` (requests + openpyxl đọc trực tiếp cell đã typed — không còn phải regex parse chuỗi markdown escape như `fleet_parser.py` cũ), `sync_from_sheet(db)` (xoá + nạp lại 3 bảng), lưu `LAST_SYNC_STATUS` (thời điểm + số dòng mỗi bảng, lỗi nếu có).
    - `main.py`: chạy sync ngay khi khởi động + tự lặp lại mỗi **5 phút** (`SHEET_SYNC_INTERVAL_SECONDS`) qua `asyncio` background task (chạy trong threadpool executor vì `requests`/`openpyxl` là blocking).
    - API mới: `POST /api/v1/fleet/sync` (đồng bộ ngay), `GET /api/v1/fleet/sync-status` (thời điểm đồng bộ gần nhất).
    - Frontend: nút "Cập nhật dữ liệu" ở topbar giờ mở modal (`ImportModal.jsx`) có nút **"Đồng bộ ngay từ Google Sheet"** thật (gọi `/sync`), hiển thị thời điểm đồng bộ gần nhất — không còn chỉ là hướng dẫn chạy script tay.
    - Dependency mới: `openpyxl==3.1.5`, `requests==2.34.2` trong `requirements.txt`.
    - `fleet_parser.py` + `scripts/seed_fleet_data.py` + `backend/data/fleet_sheet_raw.md` (đường nạp dữ liệu tĩnh cũ) **vẫn giữ lại làm phương án dự phòng** (nếu sheet đổi quyền chia sẻ hoặc mất mạng) — không còn là đường nạp chính, `main.py` không gọi tới nữa.
    - Rủi ro cần biết: nếu người sở hữu Sheet đổi link từ "công khai" sang "riêng tư", cơ chế sync này sẽ lỗi (`/sync-status` sẽ có `error`) — khi đó cần quay lại quy trình snapshot thủ công hoặc thiết lập service account.
  - **Cập nhật (2026-07-28) — làm sâu 3 màn còn "tên đúng nhưng nội dung rút gọn" so với mockup gốc:**
    - `Theo dõi bảo dưỡng` (`/fleet/theo-doi-bao-duong`) → `MaintenanceTrack.jsx` (mới): donut tuân thủ, xu hướng đúng hạn/chi phí/số lượt theo THÁNG THẬT (data giờ trải dài 2025-01 → 2026-07, ~11+ tháng), độ lệch km theo bộ phận/hãng xe, top trễ/sớm hạn. Nhúng lại `MaintenanceSchedule.jsx` (list ưu tiên cũ) ở cuối trang qua prop `embedded` — không xoá code cũ, ghép làm phần "Kế hoạch bảo dưỡng".
    - `Theo dõi sửa chữa` (`/fleet/theo-doi-sua-chua`) → `RepairTrack.jsx` (mới): KPI, chi phí theo bộ phận/hãng xe, xu hướng theo tháng, tần suất theo loại việc, Top 20 chi phí cao/Top 20 sửa nhiều lần. Nhúng `RepairLog.jsx` cũ (có search+phân trang) ở cuối qua prop `embedded`.
    - `So sánh đội xe` — **thêm lại vào sidebar** (route `/fleet/so-sanh-doi-xe`, khác với modal "So sánh xe" ở topbar — 2 tính năng riêng, không trùng): `FleetCompareFull.jsx` ghép dữ liệu từ 3 API có sẵn (`/health-scores`, `/performance`, `/vehicles`) ở phía client, có filter bộ phận/hãng xe, 4 bảng Top 20 (chi phí, số lần SC, chi phí/km — ghi rõ là ước tính trọn đời xe theo ODO hiện tại, giờ nằm xưởng).
    - Backend thêm `get_maintenance_track()` + `get_repair_track()` trong `fleet_service.py`, endpoint `GET /api/v1/fleet/maintenance-track` và `GET /api/v1/fleet/repair-track` (group theo tháng từ `entry_date`, join `Vehicle.brand` qua `plate_number` cho phần "theo hãng xe").
    - Pattern `embedded` prop (ẩn `<h1>`/tiêu đề khi nhúng vào trang khác) — dùng lại nếu cần ghép thêm trang con sau này, tránh phải viết lại logic fetch/table đã có.
  - **Cập nhật (2026-07-28) — khớp lại đúng bố cục trang Tổng quan theo ảnh chụp mockup (4 hàng, không phải hàng tự thêm trước đó):**
    - Hàng 1-2 (10 KPI): đổi đúng thứ tự/nhãn ảnh — thêm 3 chỉ số mới **Tổng chi phí tuần/tháng/năm** tính thật theo ngày hệ thống hiện tại (7 ngày gần nhất / tháng hiện tại / năm hiện tại), thay cho cách tính "tuần gần nhất có dữ liệu" trước đó.
    - Hàng 3: Trạng thái hoạt động xe (donut) · Chi phí sửa chữa theo tháng (lấy từ `repair-track`) · Cảnh báo quan trọng — giữ nguyên.
    - Hàng 4 (mới thêm, trước đây thiếu hẳn): **Sức khỏe đội xe** (donut 3 mức Khỏe mạnh ≥80/Cần theo dõi 50-79/Nguy cơ <50, tính từ `health-scores` thật) · **Chi phí vận hành theo tháng** (stacked bar theo hạng mục Bảo dưỡng/Sửa chữa/Thay lốp/Khác, tách từ `work_type` thật) · **Tổng ODO ghi nhận theo tháng** (tổng cột ODO trong các lượt BTBD mỗi tháng — có ghi chú rõ đây KHÔNG phải quãng đường thực đi trong tháng, vì Sheet không có odo-delta theo kỳ).
    - Backend thêm `get_overview_extra()` + endpoint `GET /api/v1/fleet/overview-extra` (cost theo tuần/tháng/năm, stacked category theo tháng, tổng ODO theo tháng, health buckets).
    - Component mới `StackBars.jsx` (stacked bar chart, port từ hàm `stackBars` trong mockup).
    - **Chủ động KHÔNG làm**: sparkline + % delta trên từng KPI card như ảnh — vì các chỉ số dạng "số lượng hiện tại" (Tổng số xe, Xe hoạt động...) không có lịch sử snapshot theo ngày để tính delta thật; làm giả sẽ vi phạm nguyên tắc không bịa số. Nếu sau này cần, phải lưu snapshot định kỳ (cron chụp số liệu mỗi ngày) rồi mới tính được delta/sparkline thật.
    - Đã bỏ lại các card tự thêm ở lần trước ("Xe điểm sức khỏe thấp nhất", "Xe nằm xưởng nhiều nhất", "Chi phí theo Gara") vì không có trong ảnh mockup — nội dung đó vẫn xem được đầy đủ ở các trang con (Điểm sức khỏe xe, Hiệu suất đội xe).
  - **Cập nhật (2026-07-28) — mặc định biểu đồ theo tháng chỉ hiện 6 tháng gần nhất** (trước đó hiện hết ~13-19 tháng, trục X quá dày khó đọc): thêm `lastMonths(arr, n=6)` trong `theme.js`, áp dụng cho toàn bộ chart theo tháng ở Tổng quan, Theo dõi bảo dưỡng, Theo dõi sửa chữa (KHÔNG áp dụng cho trang Báo cáo — trang đó cố ý xem toàn bộ lịch sử). Mỗi h4 biểu đồ có ghi rõ "(6 tháng gần nhất)".
  - **Hạn chế đã biết, CHƯA xử lý**: bộ lọc "Khoảng thời gian (ngày vào xưởng)" ở FilterBar toàn cục hiện KHÔNG có tác dụng lên các biểu đồ theo tháng ở Tổng quan/Theo dõi BD/Theo dõi SC — 3 endpoint `/overview-extra`, `/repair-track`, `/maintenance-track` chưa nhận tham số ngày lọc. Người dùng chọn ngày trên UI nhưng biểu đồ không đổi — cần bổ sung `from`/`to` param cho các endpoint này rồi truyền `filters.from/to` từ `FleetContext` vào nếu muốn khoảng thời gian lọc thật sự có tác dụng.
  - **Cập nhật (2026-07-29) — tinh chỉnh trực quan trang Tổng quan sau nhiều vòng phản hồi (biểu đồ quá to → quá thô/dẹt → lại quá hẹp/không cân đối):**
    - `Bars.jsx`/`GroupedBars.jsx`: chốt tỉ lệ viewBox `580x190` (giữa mức 226 ban đầu "quá to" và 140 "quá dẹt"). `autoScale()` tổng quát hoá cho MỌI đơn vị (trước chỉ auto quy đổi triệu/tỷ cho "đ") — tránh lỗi hiện số thô kiểu "500000000" trên trục biểu đồ ODO (đơn vị "km").
    - `StackBars.jsx` (stacked) đã bị xoá từ trước, thay bằng `GroupedBars.jsx` (cột nhóm cạnh nhau) — tránh nhầm baseline khi so sánh giữa các tháng.
    - Bố cục 6 card (Trạng thái hoạt động xe / Chi phí sửa chữa / Cảnh báo quan trọng / Sức khỏe đội xe / Chi phí vận hành / Tổng ODO) chốt thành **lưới 2 hàng × 3 cột, tỉ lệ cột `0.8fr 1.5fr 1.1fr`** (donut hẹp nhất, 2 biểu đồ chi phí ở giữa rộng nhất, cột Cảnh báo/ODO nới ra theo yêu cầu). Cả 2 hàng dùng `alignItems:'stretch'` để nền card phủ đều hết chiều cao hàng (trước đó dùng `'start'` để lại khoảng trắng thừa dưới card ngắn) — nội dung donut/alert/ODO canh giữa dọc trong phần cao thêm đó; 2 card biểu đồ chi phí giữ nguyên vì chúng là chuẩn chiều cao hàng.
    - Card "Cảnh báo quan trọng" đổi từ danh sách 3 dòng/mục (rất cao) sang **1 dòng/mục gọn** + `flex:1, overflowY:'auto'` để tự co giãn khớp chiều cao hàng thay vì dùng `maxHeight` cứng.
    - Donut "Sức khỏe đội xe": tâm đổi từ hiển thị tổng số xe (207) sang **điểm sức khỏe trung bình thật** (tính từ field `score` của `/health-scores`, lọc theo bộ lọc hiện tại — biến `avgHealthScore`/`health_f` trong `Overview.jsx`); mỗi dòng chú thích thêm % số xe theo mức, đồng bộ cách hiển thị với donut "Trạng thái hoạt động xe".
    - Legend 2 donut chuyển từ nằm ngang cạnh donut sang **xếp dọc phía dưới donut** để card hẹp lại theo chiều ngang, nhường không gian cho cột Cảnh báo/ODO.
  - **Cập nhật (2026-07-29) — 2 tính năng ẩn/hiện mới cho layout gọn hơn:**
    - **Sidebar thu gọn**: `FleetLayout.jsx` thêm state `collapsed` (persist `localStorage['fleet_sidebar_collapsed']`) + nút tròn 26px đè lên cạnh phải sidebar (icon `ph-caret-left`/`ph-caret-right`). Khi thu gọn: `<aside>` co từ 244px → 76px, ẩn hẳn `<nav>` (toàn bộ menu), chỉ còn lại phần logo "FLEET OPS/Quản lý Đội xe" (ẩn phần chữ, giữ icon logo căn giữa).
    - **Thanh bộ lọc ẩn/hiện**: thêm `filterBarOpen`/`setFilterBarOpen` vào `FleetContext.jsx` (persist `localStorage['fleet_filterbar_open']`, mặc định ẩn). `FilterBar.jsx` return `null` khi `!filterBarOpen`. Nút bật/tắt là icon phễu (`ph-bold ph-funnel` — đã đổi từ `ph-duotone ph-funnel-simple` vì nét mảnh khó nhìn) đặt cạnh chuông thông báo trên `TopBar.jsx`, đổi màu xanh khi đang mở + có chấm đỏ báo khi đang áp dụng bộ lọc (`hasFilters`).
    - Lưu ý: `dashboard/md` KHÔNG phải git repo (`Test-Path .git` → false) — không có bước commit/push, mọi thay đổi chỉ tồn tại trên đĩa cục bộ. Nếu cần versioning, phải tự `git init` trước.
  - **Cập nhật (2026-07-29) — đồng bộ Sheet mỗi 1 phút + nút "Làm mới" đồng bộ thật:** `SHEET_SYNC_INTERVAL_SECONDS` trong `main.py` giảm từ 5 phút → **60 giây**. Nút "Làm mới" ở `TopBar.jsx` trước đây chỉ `window.location.reload()` (đọc DB cũ) — giờ gọi `syncSheet()` (POST `/sync`) đồng bộ trực tiếp từ Google Sheet trước, xong mới reload; có state `refreshing` disable nút + icon xoay (`@keyframes flt-spin`, thêm mới trong `fleet-theme.css`) trong lúc chờ.
  - **Cập nhật (2026-07-29) — nâng cấp ô tìm nhanh biển số ở TopBar thành tính năng thật, bỏ mọi placeholder giả:**
    - Placeholder đổi thành "Nhập biển số xe...", bỏ chữ "/5 số cuối" (trước đó ghi vậy nhưng backend KHÔNG hề hỗ trợ tìm theo số cuối — chỉ so khớp tuyệt đối `plate_number == ...`, là claim sai/bịa UI).
    - **Tra cứu theo số cuối giờ THẬT**: `fleet_service.get_vehicle_lookup()` — nếu không khớp tuyệt đối và độ dài input ≥4, thử `plate_number ILIKE '%<input>'` (kết thúc bằng input đó); nếu khớp đúng 1 xe → dùng luôn; nếu khớp nhiều xe → raise lỗi rõ liệt kê các biển số khớp để người dùng nhập đầy đủ hơn (không đoán bừa 1 trong nhiều kết quả).
    - **Tự động gợi ý (autocomplete) khi gõ**: `TopBar.jsx` debounce 250ms gọi `fetchVehicles({search})` (endpoint có sẵn, dùng `ILIKE '%...%'`), hiện dropdown tối đa 8 gợi ý (biển số + hãng/model + bộ phận), bấm vào điều hướng thẳng tới `/fleet/tra-cuu`.
    - **Mic tìm bằng giọng nói — THẬT**, dùng Web Speech API (`window.SpeechRecognition`/`webkitSpeechRecognition`, `lang='vi-VN'`), không cần thư viện/server. Nếu trình duyệt không hỗ trợ (Firefox/Safari cũ) báo thật lỗi, không giả vờ hoạt động.
    - **Camera quét biển số — THẬT**, dùng package mới `tesseract.js@7` (OCR mã nguồn mở, chạy 100% trên trình duyệt qua WASM, KHÔNG gửi ảnh lên server). Component mới `components/PlateScanModal.jsx`: mở camera qua `getUserMedia`, có khung dẫn (dashed guide box) để người dùng canh biển số, khi chụp chỉ crop đúng vùng khung (không OCR cả khung hình → giảm nhiễu), chạy `createWorker('eng')` + `setParameters({tessedit_pageseg_mode: PSM.SINGLE_LINE, tessedit_char_whitelist: 'A-Z0-9.-'})` để tối ưu cho chuỗi biển số 1 dòng. Kết quả OCR chỉ ĐIỀN vào ô tìm kiếm (không tự điều hướng) — vì độ chính xác OCR phụ thuộc ánh sáng/góc chụp, để người dùng tự kiểm tra lại trước khi Enter, tránh dẫn sai xe do tin tưởng OCR 100%.
    - Rủi ro cần biết: `getUserMedia` yêu cầu HTTPS hoặc `localhost` (secure context) — nếu deploy thật trên LAN qua HTTP, camera sẽ bị trình duyệt chặn; khi đó cần HTTPS hoặc chấp nhận chỉ dùng được trên localhost/HTTPS.
    - **Gotcha StatReload nặng hơn dự đoán**: sau khi sửa `fleet_service.py`, chỉ `Stop-Process` đúng 2 PID (reloader + server con) rồi start lại **KHÔNG ĐỦ** — server mới vẫn chạy logic CŨ (đã tự kiểm chứng: gọi trực tiếp hàm qua `python -c` cho kết quả đúng, nhưng gọi qua HTTP vào server đang chạy vẫn trả 404 như code cũ). Chỉ hết hẳn sau khi `Get-Process python | Stop-Process -Force` (diệt TOÀN BỘ tiến trình python, không chỉ 2 PID đã biết) rồi mới start lại. Từ nay luôn diệt hết toàn bộ `python.exe` trước khi restart backend, đừng chỉ kill theo PID đã biết.
  - **Cập nhật (2026-07-29) — sửa lỗi thật về múi giờ (không chỉ là format hiển thị):** phát hiện `services/sheet_sync.py` dùng `datetime.utcnow()` cho `synced_at` — dù máy Windows này đã đặt múi giờ hệ thống đúng "SE Asia Standard Time (UTC+07:00)", việc gọi `utcnow()` vẫn LUÔN trả giờ UTC trần, lệch 7 tiếng so với giờ Hà Nội thật (đã tự kiểm chứng: `Get-Date` máy = 01:53, nhưng `synced_at` trả về mang giờ 18:53 hôm trước) — khiến thanh đếm ngược tự động làm mới ở `SyncIndicator.jsx` có thể hiển thị sai nếu trình duyệt parse chuỗi ISO không có offset theo kiểu khác máy chủ.
    - Thêm `core/timezone.py` — hàm `now_hanoi()` dùng `zoneinfo.ZoneInfo("Asia/Ho_Chi_Minh")`, KHÔNG phụ thuộc múi giờ hệ điều hành máy chủ (để nếu sau này deploy lên máy chủ đặt UTC vẫn ra đúng giờ Hà Nội). Thay `datetime.utcnow()`/`datetime.now()` bằng `now_hanoi()` ở mọi nơi tính giờ "hiện tại" cho nghiệp vụ hiển thị: `sheet_sync.py` (`synced_at`), `fleet_service.py` (`_days_remaining`, `get_overview_extra` — chi phí tuần/tháng/năm). CHỦ Ý KHÔNG đổi `core/security.py` (hạn JWT) và `models/base.py` (`deleted_at` soft-delete) — 2 chỗ đó là nội bộ hệ thống/audit, quy ước dùng UTC là chuẩn thông thường, không phải thứ người dùng nhìn thấy.
    - **Gotcha mới phát sinh**: `zoneinfo.ZoneInfo("Asia/Ho_Chi_Minh")` bị lỗi `ZoneInfoNotFoundError` trên Windows vì Python 3.12 trên Windows KHÔNG có sẵn IANA tzdata (khác Linux thường có sẵn) — phải cài thêm package `tzdata` (`pip install tzdata`, đã thêm vào `requirements.txt`) thì `zoneinfo` mới tra được múi giờ.
    - `get_overview_extra()`: `now_hanoi()` trả về datetime CÓ tzinfo, nhưng các cột ngày trong DB (`entry_date`...) là datetime KHÔNG có tzinfo (giá trị thô từ Excel) — so sánh trực tiếp 2 loại này ở Python sẽ crash `TypeError`. Phải `.replace(tzinfo=None)` ngay sau `now_hanoi()` để giữ đúng giờ Hà Nội nhưng vẫn ở dạng naive, khớp kiểu với dữ liệu DB.
    - Frontend `theme.js`: viết lại `fmtDate()` — ưu tiên regex cắt thẳng `YYYY-MM-DD` từ chuỗi gốc (không qua `Date`/múi giờ) vì các mốc hạn giấy tờ chỉ là NGÀY LỊCH, quy đổi qua timezone có thể lệch ngày ở biên; luôn xuất `dd/mm/yyyy` cố định (không dùng `toLocaleDateString('vi-VN')` vì phụ thuộc locale trình duyệt, không đảm bảo format). Thêm `fmtDateTime()` riêng cho mốc THỜI ĐIỂM thật (đồng bộ dữ liệu) — dùng `Intl.DateTimeFormat` với `timeZone:'Asia/Ho_Chi_Minh'` ép cứng, hiển thị đúng giờ Hà Nội bất kể múi giờ máy người xem. Đã áp `fmtDate` cho toàn bộ cột `expiry_raw` trước đó hiển thị chuỗi thô chưa qua format (`Documents.jsx`, `RegistrationPlan.jsx`, `VehicleDetail.jsx`, `PlateLookup.jsx`, `Overview.jsx`).
  - **Cập nhật (2026-07-29) — BUG nghiêm trọng: toàn bộ trạng thái hạn giấy tờ tính sai từ trước tới giờ, người dùng phát hiện qua ảnh chụp "Hồ sơ giấy tờ" (1237/1242 mục báo "Đã hết hạn" dù ngày hết hạn thật còn rất xa, có mục tận 2032):**
    - Nguyên nhân: `_days_remaining()` trong `fleet_service.py` parse `raw_value` bằng format `"%d/%m/%Y"`, nhưng dữ liệu THẬT lưu trong cột `inspection_expiry`/`road_fee_expiry`/... (khai báo `Column(String)` trong `models/vehicle.py`) lại ở format `"%Y-%m-%d %H:%M:%S"` (do `sheet_sync.py`'s `_to_str()` chỉ gọi `str(datetime_obj)` khi ghi vào DB, ra chuỗi kiểu "2026-08-18 00:00:00"). → `strptime` LUÔN ném `ValueError` → hàm luôn rơi vào nhánh `except: return -1` → MỌI giấy tờ đều bị coi là "quá hạn 1 ngày" bất kể ngày thật.
    - Bug này có tồn tại từ trước, không phải do các thay đổi múi giờ/format ngày gần đây gây ra — chỉ là người dùng tình cờ phát hiện ra khi đối chiếu cột "Ngày hết hạn" (đã format đúng dd/mm/yyyy) với cột "Còn lại"/"Trạng thái" (vẫn tính sai).
    - **Ảnh hưởng lan rộng hơn ảnh chụp cho thấy**: cùng hàm `_days_remaining()` được dùng ở `get_fleet_alerts()` (cảnh báo giấy tờ ở Tổng quan/AI Analytics), `get_vehicle_lookup()` (Hồ sơ chi tiết xe/Tra cứu biển số), và quan trọng nhất — `get_health_scores()` (trọng số 15% "hồ sơ giấy tờ" trong công thức điểm sức khỏe) → **điểm sức khỏe của TOÀN BỘ 207 xe bị tính thấp hơn thật** suốt thời gian qua vì hệ thống nghĩ xe nào cũng có giấy tờ hết hạn.
    - Fix: `_days_remaining()` thử lần lượt nhiều format (`"%Y-%m-%d %H:%M:%S"` trước tiên — đúng format thật, rồi `"%Y-%m-%d"`, `"%d/%m/%Y %H:%M:%S"`, `"%d/%m/%Y"` để không vỡ nếu nguồn dữ liệu đổi sau này).
    - Đã tự kiểm chứng qua API thật sau fix: biển `50H00214` — "Hạn đăng kiểm" 18/08/2026 → `days_remaining: 20, doc_status: "soon"` (đúng), "Hạn phù hiệu" 05/05/2032 → `days_remaining: 2107, doc_status: "ok"` (đúng, trước đó cũng bị báo "đã hết hạn"!). Thống kê tổng `/documents` sau fix: 1242 mục → 106 thật sự hết hạn, 28 sắp hết hạn (≤30 ngày, theo `expiry_alert_window_days` mặc định trong `app_settings`), 1108 còn hạn — hợp lý hẳn so với 1237/1242 "hết hạn" giả trước đó.
    - Ngưỡng "sắp hết hạn" hiện vẫn là 30 ngày (cấu hình được ở trang Cài đặt, bảng `app_settings.expiry_alert_window_days`) — người dùng nêu ví dụ "20 ngày" trùng đúng case biển 50H00214 họ đang xem (18/08/2026 cách ngày họ xem đúng 20 ngày), KHÔNG phải yêu cầu đổi ngưỡng mặc định từ 30 xuống 20 — chưa đổi ngưỡng, chỉ sửa bug tính toán.
  - **Cập nhật (2026-07-29) — đồng bộ hiển thị biển số xe (BSX) trên toàn app:** thêm component chung `components/PlateLink.jsx` (`<Link to="/fleet/xe/:plate" className="plate-link">`) + class `.fleet-shell .plate-link` trong `fleet-theme.css` (màu `var(--color-text)` đen, bỏ gạch chân, gạch chân lại khi hover; có sẵn biến `--plate-color` + class phụ `.on-dark` để tự đổi màu trắng nếu sau này đặt trong khối nền tối). Áp dụng cho MỌI nơi hiển thị BSX dạng bảng/danh sách trước đó là text thường hoặc span màu tay: `VehicleList`, `Documents`, `RepairLog`, `Roadside`, `RegistrationPlan`, `MaintenanceSchedule`, `MaintenanceTrack` (top trễ/sớm), `RepairTrack` (top chi phí/tần suất), `FleetCompareFull` (4 bảng rank), `Performance` (cả 2 bảng). Các `<Link>` BSX có sẵn từ trước (Overview cảnh báo, SmartAlerts, HealthScore) chỉ thêm `className="plate-link"`, giữ nguyên style riêng (size/weight) từng nơi. Cố ý KHÔNG đổi bảng BSX trong `CompareModal.jsx` (2 chỗ) — 1 là checkbox chọn xe so sánh (bấm để toggle chọn, không phải điều hướng), 1 là bảng preview trong modal — biến chúng thành link điều hướng sẽ rời trang mà modal vẫn mở, gây rối UX.
  - **Cập nhật (2026-08-04/05) — thử đưa dashboard lên "online" để xem hàng ngày, CHƯA có link ổn định lâu dài:**
    - Phát hiện tiến trình chạy nền khởi động bằng `Start-Process -WindowStyle Hidden` trong PowerShell tool **không sống sót** giữa các lượt gọi tool khác nhau (backend/frontend tự "biến mất" nhiều lần trong session dù log show start thành công) — phải chuyển sang chạy lệnh server trực tiếp với cờ `run_in_background: true` của chính PowerShell tool (không qua `Start-Process`) mới giữ tiến trình sống ổn định qua nhiều lượt.
    - Sửa 2 vấn đề kiến trúc để app truy cập được từ máy khác (không chỉ `localhost`):
      1. `frontend/src/fleet/api.js`: `API_BASE` đổi từ hard-code `http://localhost:8000/...` sang đường dẫn tương đối `/api/v1/fleet`.
      2. `frontend/vite.config.js`: thêm `proxy: {'/api': 'http://localhost:8000'}` cho cả `server` (dev) và `preview` — nhờ đó frontend + backend gộp chung 1 origin/1 cổng, không bị lỗi gọi nhầm API về "localhost" của máy người xem, và chỉ cần tunnel/mở 1 cổng duy nhất (3000) ra ngoài.
      3. Thêm `preview.allowedHosts` (Vite 5 chặn Host header lạ theo mặc định — phải khai báo `.trycloudflare.com` mới cho phép mở qua tunnel).
    - **Đã thử Cloudflare Tunnel** (theo lựa chọn của người dùng): cài `cloudflared` qua `winget` (không có sẵn, phải cài) — path thật: `C:\Program Files (x86)\cloudflared\cloudflared.exe` (winget KHÔNG tự thêm vào PATH của phiên PowerShell hiện tại, phải gọi full path).
      - **Quick Tunnel (không cần tài khoản)**: chạy được ngay, nhưng bản đầu dùng protocol mặc định `quic` (UDP) — sau ~37 phút bị rớt kết nối liên tục ("wsasendto: A socket operation was attempted to an unreachable network", "no recent network activity") và KHÔNG tự hồi phục — trùng thời điểm IP LAN của máy đổi từ `192.168.0.137` → `192.168.1.13` (nghi router DHCP cấp lại IP / mạng máy chủ không ổn định). Fix: chạy `cloudflared tunnel --protocol http2 --url ...` (ép dùng HTTP/2 qua TCP thay vì QUIC/UDP) — ổn định hơn hẳn.
      - **Named Tunnel (link cố định, gắn domain riêng)**: cần `cloudflared tunnel login` — mở trình duyệt xác thực Cloudflare rồi ghi `cert.pem` vào `C:\Users\Administrator\.cloudflared\cert.pem`. Đã thử **2 lần, cả 2 lần đều lỗi giống nhau**: treo ở "Waiting for login..." đúng khoảng 8 phút rồi báo `Failed to write the certificate... error="Failed to fetch resource"`. Không rõ do người dùng chưa bấm Authorize kịp hay do mạng máy chủ (nghi vấn cao hơn, vì lỗi lặp lại gần như đúng thời điểm cả 2 lần, và máy này đã có dấu hiệu mạng không ổn định từ vụ QUIC ở trên) — **CHƯA XÁC ĐỊNH ĐƯỢC NGUYÊN NHÂN GỐC, CHƯA GIẢI QUYẾT**.
      - Do lỗi trên, **hiện KHÔNG có link online ổn định lâu dài nào đang chạy**. Quick tunnel HTTP/2 (`https://camp-tue-peace-verde.trycloudflare.com`) đã dùng để demo tạm nhưng theo đúng điều khoản Cloudflare thì loại tunnel này KHÔNG có SLA/uptime, và link sẽ đổi mỗi lần cloudflared phải khởi động lại — không dùng để "xem hàng ngày" lâu dài được.
    - **Thử "gửi file dev" qua Google Drive KHÔNG khả thi**: đã thử `mcp__claude_ai_Google_Drive__create_file` với `base64Content` — file zip export chỉ 0.54MB nhưng base64 hoá thành ~730KB text, mà base64 tokenize rất tệ (~1 token/ký tự) → vượt giới hạn 25.000 token của tool `Read` khi cần nạp lại nội dung vào context để gọi tool, không thể gửi qua kênh chat được. **Bài học: không dùng đường base64-qua-context để chuyển file >~vài chục KB — chỉ khả thi nếu người dùng có quyền truy cập filesystem trực tiếp (đúng trường hợp ở đây: người dùng dùng VS Code nối trực tiếp vào máy này, xác nhận thấy `D:\GHN\BTBD code` trong sidebar) hoặc có sẵn 1 endpoint upload thật (link Cloudflare R2/S3 presigned, Google Drive folder họ đã share quyền ghi...).**
    - Export gọn (đã tạo, còn trên máy): `C:\Users\Administrator\AppData\Local\Temp\BTBD-dashboard-dev.zip` — zip source code (backend+frontend, có kèm `backend/internal_app.db` snapshot dữ liệu thật hiện tại), đã loại `node_modules/dist/build/__pycache__/venv/*.log` qua `robocopy /XD .../XF *.log` rồi `Compress-Archive`. Cách làm này (robocopy loại trừ → zip thư mục staging) là cách đúng để export gọn dự án Windows có node_modules nặng, nên tái dùng nếu cần export lại.
    - **Việc cần làm tiếp nếu muốn link ổn định lâu dài**: (a) thử lại `cloudflared tunnel login` — nếu vẫn lỗi ở mốc ~8 phút thì gần như chắc là do mạng máy chủ, cần chuyển hướng dùng server/cloud khác ổn định hơn máy dev này; (b) hoặc bỏ qua Cloudflare, dùng phương án người dùng từng được đề xuất nhưng chưa chọn: server nội bộ GHN đã có sẵn, hoặc PaaS free (Render/Railway) mà chính người dùng tự bấm deploy (cần tài khoản của họ, tôi không tự làm thay được).
  - **Cập nhật (2026-08-11) — Xác thực đăng nhập + Phân quyền RBAC (3 cấp: admin/manager/viewer) cho toàn bộ Fleet Dashboard:**
    - **Quyết định đã chốt với người dùng** (qua AskUserQuestion, không tự suy đoán): 3 cấp role admin/quản lý/viewer; tài khoản mới tự đăng ký bằng email `@ghn.vn` rồi chờ Admin duyệt (không tự động kích hoạt); mật khẩu Admin gốc tự sinh ngẫu nhiên (không tự chọn); TOÀN BỘ app (kể cả xem dữ liệu) bắt buộc đăng nhập, không riêng thao tác sửa/xoá.
    - **Backend**: `models/user.py` thêm `full_name`, `status` (pending/active/rejected — tách biệt với `is_active` vốn dùng cho khoá/mở tài khoản đang hoạt động), `must_change_password`. `core/migrate.py` (mới) — ALTER TABLE ADD COLUMN idempotent cho SQLite vì dự án chưa dùng Alembic và `create_all()` không tự thêm cột vào bảng đã tồn tại (bảng `users` cũ đã có từ trước, phải migrate tay). `core/security.py` thêm `get_current_user` (giải mã Bearer JWT, `sub`=email) + `require_roles(*roles)` factory. `schemas/auth.py` (mới) validate domain `@ghn.vn` + độ dài mật khẩu ≥8 bằng Pydantic `field_validator`. `api/auth.py` viết lại hoàn toàn: `/register`, `/login` (JSON body email+password, không còn OAuth2 form/username), `/me`, `/me/password`, và 2 API chỉ Admin: `GET /users`, `PATCH /users/{id}` (duyệt/từ chối/đổi role — có chặn Admin tự hạ quyền chính mình). `api/fleet.py`: `APIRouter(dependencies=[Depends(get_current_user)])` ở cấp router để bắt buộc đăng nhập cho MỌI endpoint (tránh sót route nếu gắn thủ công từng cái); `PUT /settings` thêm `require_roles("admin")`, `POST /records` thêm `require_roles("admin","manager")`. `main.py`: chạy `run_startup_migrations()` + `_seed_admin_account()` (idempotent — chỉ tạo 1 lần) khi khởi động, in mật khẩu tạm ra console đúng 1 lần (không lưu plaintext ở đâu khác).
    - **Gotcha lớn phát sinh khi seed**: `print()` thường (không `flush=True`) khi chạy `python main.py` qua PowerShell tool với `run_in_background` bị BUFFER, không hiện trong file log dù xem lại nhiều lần — phải chạy `python -u main.py` (hoặc set `PYTHONUNBUFFERED=1`) mới thấy được. Ngay cả sau khi bật `-u`, log tiếng Việt qua console Windows vẫn hiện SAI ENCODING (đọc được lõm bõm, không phải lỗi thật — chỉ là codepage PowerShell). Vì lần seed đầu không đọc được mật khẩu rõ ràng từ log, đã phải viết script riêng (`reset_admin_password.py`) UPDATE thẳng cột `hashed_password` qua `passlib` + `sqlite3` để đặt lại mật khẩu Admin và ghi ra 1 file `.txt` riêng đọc chắc chắn được — **bài học: đừng phụ thuộc vào việc đọc lại console log tiếng Việt trên Windows để lấy thông tin nhạy cảm 1-lần (mật khẩu, token...), luôn ghi thẳng ra file UTF-8 riêng hoặc query DB trực tiếp để xác nhận.**
    - **Mật khẩu Admin cuối cùng sau khi dọn dữ liệu test**: email `linhnd1@ghn.vn` / mật khẩu `lN35qw8BeD3w` (role=admin, `must_change_password=true` — hệ thống sẽ tự bắt đổi mật khẩu ở lần đăng nhập đầu tiên qua modal không thể tắt). Đã tự kiểm chứng lại bằng `POST /auth/login` thật trả 200 trước khi báo cho người dùng.
    - **Đã tự test toàn bộ flow thật qua API** (script `test_auth_flow.py`, không chỉ đọc code): login admin OK, gọi API fleet không token → 401, có token → 200; đăng ký email không phải `@ghn.vn` → 422; đăng ký `@ghn.vn` → 201 pending; login khi đang pending → 403; Admin duyệt (`PATCH status=active`) → login được; viewer gọi `POST /records` → 403; Admin đổi role viewer→manager → login lại → `POST /records` → 201; manager gọi `PUT /settings` → 403 (chỉ admin); đổi mật khẩu → `must_change_password` về `false`; sai mật khẩu → 401 thông báo chung (không lộ email nào tồn tại — chống dò email kiểu OWASP User Enumeration). Dữ liệu test (`test.viewer@ghn.vn`, record biển `TEST-001`) đã xoá sạch khỏi DB thật sau khi test xong.
    - **Frontend**: `fleet/httpClient.js` (mới) — thay thế vai trò "Axios Interceptor" bằng wrapper trên `fetch` gốc (`apiFetch`/`apiGet`/`apiSend`, dự án không thực sự dùng axios dù có trong `package.json`): tự đính `Authorization: Bearer`, tự phát `CustomEvent('fleet:session-expired')` khi 401 để tách rời khỏi React Router. `fleet/authApi.js` (mới) gọi các API auth. `fleet/api.js` sửa lại toàn bộ để dùng `apiGet`/`apiSend` thay vì `fetch` trần (giờ mọi request đều tự có token). `fleet/auth/` (thư mục mới): `AuthContext.jsx` (có `ready` flag tách biệt với `user` để tránh nháy redirect trong lúc xác thực lại token với server khi tải trang), `ProtectedRoute.jsx` (hỗ trợ giới hạn `roles`), `LoginPage.jsx`, `RegisterPage.jsx`, `ChangePasswordModal.jsx` (có chế độ `forced` không cho đóng — dùng khi `must_change_password=true`). `App.jsx`: bọc `<AuthProvider>` quanh `<Router>`, `/fleet/login` + `/fleet/register` nằm NGOÀI `FleetLayout` (không sidebar), route `/fleet` bọc `ProtectedRoute`, riêng `phieu-bao-duong` bọc thêm `roles={['admin','manager']}`. `FleetLayout.jsx`: thêm `filterNavByRole()` ẩn hẳn mục menu "Phiếu bảo dưỡng" khỏi Viewer (không chỉ chặn route), render `<ChangePasswordModal forced>` khi `user.must_change_password`. `TopBar.jsx`: thêm `UserMenu` (avatar chữ cái đầu tên, badge vai trò tiếng Việt, nút Đổi mật khẩu/Đăng xuất). `Settings.jsx`: khoá `<fieldset disabled>` toàn bộ form ngưỡng cảnh báo cho non-admin (chỉ đọc), thêm `UserManagementPanel.jsx` (mới, chỉ hiện với Admin) — bảng duyệt/từ chối/đổi role từng tài khoản qua `DataTable` có sẵn, tự chặn Admin đổi role chính mình ở UI (khớp rule Backend).
    - Đã chạy `npm run build` (pass, 130 module, không lỗi) trước khi khởi động lại dev server — chưa tự chụp màn hình xác nhận UI (vẫn thiếu công cụ trình duyệt headless như các module trước), nên mở `http://localhost:3000/fleet/login` kiểm tra trực quan 1 lần trước khi tin tưởng hoàn toàn giao diện.
    - **Rủi ro/nợ kỹ thuật còn lại, CHƯA xử lý**: `SECRET_KEY` JWT vẫn đang dùng giá trị mặc định hardcode trong `core/security.py` (chỉ in cảnh báo ra console, chưa chặn cứng) — cần đặt biến môi trường `SECRET_KEY` thật trong `.env` trước khi đưa ra ngoài máy dev này, nếu không mọi token đều có thể bị giả mạo bởi ai đọc được source code. Token JWT sống 8 tiếng, không có refresh token — hết hạn phải đăng nhập lại từ đầu (chưa làm silent refresh).
  - **SỰ CỐ NGHIÊM TRỌNG (2026-08-12) — `sheet_sync.py` (auto-sync mỗi 60s) đã tự XOÁ SẠCH dữ liệu thật trong DB do lỗi #REF! ở Google Sheet gốc, đã khôi phục nhưng mất ~1 tuần dữ liệu mới nhất:**
    - **Người dùng báo "không update được dữ liệu"** → kiểm tra log backend thấy auto-sync (chạy mỗi 60s từ `main.py`) liên tục ghi `records: 1, vehicles: 0, statuses: 0` — tức MỖI LẦN sync đều XOÁ 3 bảng (`db.query(...).delete()`) rồi nạp lại gần như RỖNG, vì code cũ không kiểm tra dữ liệu tải về có hợp lý hay không trước khi ghi đè. DB thật đã bị xoá xuống còn 0 xe/1 record/0 status trước khi phát hiện ra.
    - **Truy tìm nguyên nhân gốc (không chỉ vá triệu chứng)**: viết script gọi trực tiếp `sheet_sync.fetch_workbook()` và đọc thô từng dòng — phát hiện **bản thân Google Sheet gốc bị lỗi**: toàn bộ cột dữ liệu (BKS ở "Lịch sử Bảo dưỡng - sửa chữa" 14.526 dòng, Biển số ở "Data xe" 1001 dòng) đều trả về `None` ở MỌI dòng, TRỪ dòng 2 có giá trị literal `'#REF!'` (dòng 2 lại vô tình PASS qua filter `not row[0]` vì `'#REF!'` là string không rỗng → đúng lý do ra con số "1 record"). Đây là dấu hiệu kinh điển của formula tham chiếu bị vỡ (ai đó xoá 1 dòng/cột/sheet mà công thức tham chiếu tới) khiến TOÀN BỘ cột tính toán trong cả 2 sheet cascade lỗi — **lỗi nằm ở phía Google Sheet nguồn (do team vận hành GHN chỉnh sửa), không phải bug code phía mình.** Đã loại trừ nguyên nhân network/timeout: tải trực tiếp vẫn ra đúng `max_row=14526`/`max_row=1001` (kích thước file đúng), chỉ có NỘI DUNG ô là rỗng.
    - **Việc CẦN LÀM tiếp — không tự sửa được từ code**: phải báo cho người quản lý Sheet "M12 Data bot btbd 1" (owner `nguyenduylinhauto@gmail.com`) kiểm tra lại công thức bị lỗi `#REF!` (khả năng cao ai đó vừa xoá 1 dòng/cột hoặc 1 sheet tham chiếu) — vào Google Sheets bấm Ctrl+Z hoặc xem Lịch sử phiên bản (File → Xem lịch sử phiên bản) để tìm đúng thời điểm hỏng và khôi phục công thức. **Chưa có cách nào tự động phát hiện Sheet đã được sửa xong** — sau khi họ báo đã sửa, phải tự chạy lại `POST /api/v1/fleet/sync` (nút "Làm mới" trên UI) và kiểm tra `sync-status` không còn field `error` thì mới coi là ổn.
    - **Đã vá 2 lớp bảo vệ trong `services/sheet_sync.py`** (để KHÔNG BAO GIỜ lặp lại việc xoá nhầm dữ liệu tốt vì 1 lần tải hỏng): (1) retry tải lại tối đa 3 lần (cách nhau 8s, hằng số `MAX_FETCH_ATTEMPTS`/`RETRY_DELAY_SECONDS`) trước khi bỏ cuộc — dù thực tế lần này không giúp được gì vì lỗi nằm ở NGUỒN dữ liệu (không phải flaky network) nên cả 3 lần đều ra kết quả giống nhau; (2) **guard chặn cứng**: nếu `vehicles < 50` hoặc `records < 200` (hằng số `MIN_EXPECTED_VEHICLES`/`MIN_EXPECTED_RECORDS`, thấp hơn nhiều quy mô thật ~207/~3200 nhưng đủ cao để bắt được ca gãy toàn bộ) thì **raise lỗi và HUỶ đồng bộ trước khi đụng tới DB** — không `DELETE` gì cả. Từ giờ nếu Sheet còn lỗi, mỗi lần auto-sync 60s sẽ chỉ in lỗi ra log (`sync-status` sẽ có field `error`) và giữ nguyên DB, không phá dữ liệu cũ nữa. Cũng tăng `timeout` tải file từ 30s → 90s vì sheet log đã phình to 14.526 dòng (không phải nguyên nhân chính lần này, nhưng đúng là rủi ro thật khi sheet ngày càng lớn).
    - **Khôi phục dữ liệu đã mất**: KHÔNG có cách phục hồi lại đúng những gì đã bị xoá (không có backup tự động/WAL) — nhưng tìm lại được file zip export cũ `C:\Users\Administrator\AppData\Local\Temp\BTBD-dashboard-dev.zip` (tạo lúc 2026-08-04/05, xem mục "thử đưa dashboard lên online" phía trên) có kèm `backend/internal_app.db` snapshot lúc đó: **207 xe, 3150 record, 207 status** (dữ liệu cũ đã copy đè lên `internal_app.db` hiện tại). File DB bị xoá (0 xe/1 record) được giữ lại tại `backend/internal_app.db.broken-20260812` để tham khảo nếu cần, không xoá vội. **Mất vĩnh viễn phần dữ liệu người dùng nhập vào Sheet từ ~2026-08-05 đến trước khi Sheet bị lỗi (~2026-08-12)** — vì bản khôi phục chỉ có tới ngày 05/08 (`max(entry_date) = 2026-08-05 13:59:59`), và Sheet nguồn hiện tại không đọc được để biết chính xác họ đã nhập gì thêm trong khoảng đó. Cần báo rõ cho người dùng biết phạm vi mất dữ liệu này.
    - Tài khoản Admin bị seed lại (vì DB khôi phục từ bản 08-05 chưa có `users` — seed tạo mới với mật khẩu ngẫu nhiên mới, không phải mật khẩu đã báo trước đó): mật khẩu hiện tại `Deboo4T86zVo` — **cũng nên coi là tạm, nên đổi ngay** giống các lần trước (`must_change_password=true` đã set).
    - **Bài học quy trình quan trọng nhất**: bất kỳ luồng nào có `DELETE` rồi `INSERT` lại từ nguồn ngoài (Google Sheet, API bên thứ 3...) chạy TỰ ĐỘNG theo lịch, PHẢI có bước validate "dữ liệu mới có hợp lý không" TRƯỚC khi xoá dữ liệu cũ — không được tin tưởng ngầm rằng "mở file thành công = dữ liệu đúng". Đây là lỗ hổng đã tồn tại từ khi viết `sheet_sync.py` (2026-07-28) nhưng chưa từng bị phát hiện vì Sheet nguồn luôn lành tới giờ.
  - **Cập nhật (2026-08-12) — người dùng gửi link Sheet MỚI để thay thế, nhưng không truy cập được:**
    - Sheet mới (`17vnjXCNMzvsw_Op1SXJHxocLbFYk1kxRypEPQhZQqiI`) trả `401 Unauthorized` ở mọi biến thể export (`export?format=xlsx`, cả link "Publish to web" `/pubhtml`, `/pub?output=xlsx|csv`) — đọc nội dung trang lỗi thấy dòng "Bạn phải đăng nhập để truy cập nội dung này", khác hẳn thông báo "cần quyền" thông thường. Suy đoán có cơ sở: đây là **chính sách Google Workspace domain của GHN giới hạn mức chia sẻ công khai tối đa chỉ ở "trong nội bộ tổ chức"**, không cho "Anyone on the internet" — khác với sheet cũ (thuộc Gmail cá nhân `nguyenduylinhauto@gmail.com`, không bị giới hạn này). Nếu đúng vậy, người dùng đổi lại quyền chia sẻ trên UI cũng không giải quyết được vì đây là chặn ở cấp domain, không phải cấu hình từng file.
    - Đã hỏi người dùng hướng xử lý (Service Account / hỏi IT Workspace / tạm hoãn) — **người dùng chọn tạm hoãn, giữ nguyên sheet cũ**, chưa chuyển sang sheet mới. `SHEET_ID` trong `sheet_sync.py` VẪN LÀ sheet cũ (`1E_fYmNK3TIEMt3xz7JTwG5kmttz_lKbr8xtWRYTDQOY`), chưa đổi gì.
    - Đã tự kiểm tra lại: lỗi `#REF!` ở sheet cũ (mục sự cố phía trên) **tính đến lúc này VẪN CHƯA được ai sửa** — `dump_more_rows.py` cho kết quả giống y lúc phát hiện lỗi. Guard bảo vệ đã thêm vẫn đang hoạt động đúng (chặn ghi đè, không phá thêm dữ liệu), auto-sync mỗi 60s vẫn chạy nền và tự thất bại có kiểm soát.
    - **Nếu sau này muốn thử lại sheet mới**: cách khả thi nhất là **Service Account** (tạo trên Google Cloud Console, share sheet trực tiếp cho email service account — không cần công khai, không bị chính sách domain chặn vì đây là share tường minh cho 1 người/tài khoản cụ thể, không phải "anyone with link"). Cần đổi cách gọi trong `sheet_sync.py` từ `requests.get(EXPORT_URL)` sang dùng `google-auth` + `googleapiclient`/`gspread` với file JSON key của service account.
  - **Cập nhật (2026-08-20/21) — LIÊN KẾT VỚI GITHUB (repo `linhnd1-code/btbd_dashboard`, public), CI/CD chạy thật lần đầu tiên:**
    - Máy dev này trước đó KHÔNG có `git`/`gh` CLI — đã cài qua `winget install Git.Git` + `winget install GitHub.cli` (winget không tự thêm vào PATH của phiên hiện tại, phải tự thêm `$env:PATH` mỗi phiên PowerShell mới, hoặc gọi full path `C:\Program Files\Git\cmd\git.exe`).
    - **Phạm vi repo = TOÀN BỘ `dashboard/`** (theo lựa chọn người dùng), KHÔNG chỉ `dashboard/md/` — nghĩa là gốc repo Git ≠ gốc "dự án" mà `CLAUDE.md` mô tả (framework code nằm lùi vào 1 cấp, ở `md/`). Điều này làm vỡ giả định "workflow ở `.github/workflows/` cùng cấp code" — xem mục dưới.
    - Repo tạo sẵn bởi người dùng (rỗng, chỉ có `README.md` mặc định) → mình `git init` tại `dashboard/`, `git remote add origin`, `git fetch` + `checkout -b main origin/main` để lấy đúng lịch sử README có sẵn (KHÔNG force-push, giữ lịch sử sạch). Xác thực bằng Personal Access Token người dùng cung cấp (`repo` + `workflow` scope — thiếu `workflow` sẽ bị GitHub từ chối riêng khi push thay đổi trong `.github/workflows/*.yml`, lỗi khác với thiếu quyền `repo` thông thường). Token chỉ dùng làm URL tạm thời cho từng lệnh `git push https://<token>@github.com/...` — KHÔNG lưu vào `git remote`/`.git/config` để tránh lộ khi ai đó chạy `git remote -v`.
    - **Tạo `.gitignore` gốc mới ở `dashboard/.gitignore`** (file cũ `md/.gitignore` chỉ có tác dụng cho các thư mục con trong `md/`, không che được `dashboard/uploads/` hay thứ gì ngoài `md/`) — phát hiện + sửa 2 lỗi pattern thật khi rà lại danh sách file staged trước khi commit (luôn luôn nên làm bước này trước khi commit lần đầu 1 repo có sẵn dữ liệu cũ):
      1. `*.db` không khớp `internal_app.db.broken-20260812` (file backup lỗi từ sự cố sheet_sync) — phải thêm `*.db.*`.
      2. `backend/*.log` không khớp `frontend/vite_err.log`/`preview_out.log` (log cũ từ lúc thử đưa app online) — đổi thành `*.log` áp dụng toàn repo.
      3. **Bug tiềm ẩn từ trước** (có cả ở `md/.gitignore` gốc, chưa ai phát hiện vì chưa từng là git repo): pattern `.env.*` vô tình chặn LUÔN `.env.example` (file mẫu KHÔNG chứa secret, cần được commit) — phải thêm dòng `!.env.example` phía sau để bỏ chặn riêng file này. Đã sửa ở cả `dashboard/.gitignore` và `md/.gitignore`.
    - Database (`*.db`), mọi file log, `node_modules/`, `dist/`, `__pycache__/`, `.env` thật đều bị loại đúng — chỉ 120 file thật của dự án được commit.
    - **Lỗi phát sinh #1 — GitHub Actions không tự nhận workflow**: `md/.github/workflows/ci.yml` nằm SAI CẤP (GitHub chỉ đọc `<repo-root>/.github/workflows/`, không đọc được nếu nằm lùi 1 cấp trong `md/`) — do hậu quả của việc chọn phạm vi repo = `dashboard/` thay vì `md/`. Đã `git mv` cả `.github/` (workflow + PR template) lên gốc repo, sửa lại 3 dòng path trong `ci.yml` (`backend/` → `md/backend/`, `frontend/` → `md/frontend/`). `.pre-commit-config.yaml`/`commitlint.config.js` CỐ Ý giữ nguyên ở `md/` (không bị lỗi cứng như workflow — pre-commit chỉ cần `cd md` trước khi chạy, chấp nhận được, không đáng để xáo trộn thêm).
    - **Lỗi phát sinh #2 — Push đầu tiên bị GitHub từ chối**: `refusing to allow a Personal Access Token to create or update workflow ... without workflow scope` — PAT ban đầu chỉ có scope `repo`. Phải yêu cầu người dùng vào GitHub thêm scope `workflow` cho token đang dùng (không cần tạo token mới, chỉ cần tick thêm quyền rồi Update).
    - **Lỗi phát sinh #3 — CI chạy thật lần đầu: FAIL ở bước Black/Isort** (code chưa từng qua các công cụ này suốt quá trình viết tay nhiều phiên trước đó). Chạy `black backend/` + `isort backend/` để tự sửa, rồi phát hiện tiếp 1 lỗi CẤU HÌNH THẬT trong chính `ci.yml`: bước isort của CI thiếu cờ `--profile black` (trong khi `.pre-commit-config.yaml` đã có đúng cờ này từ đầu) → Isort mặc định bẻ dòng import theo kiểu khác Black (Vertical Hanging Indent), khiến `black --check` và `isort --check-only` KHÔNG THỂ cùng pass — sửa CI thêm `--profile black` cho khớp pre-commit. **Bài học: khi 1 dự án dùng cả Black + Isort, isort LUÔN PHẢI chạy với `--profile black` (hoặc `multi_line_output=3, include_trailing_comma=True` tương đương) ở MỌI nơi (CI, pre-commit, local) — thiếu ở bất kỳ đâu cũng gây xung đột không thể tự hết bằng cách chạy lại.**
    - **Lỗi phát sinh #4 — Flake8 báo hàng loạt E501 (dòng dài)**: do rất nhiều comment tiếng Việt giải thích "TẠI SAO" (đúng yêu cầu `core_principles.md`) dài hơn 88 ký tự — Black CHỦ Ý không bẻ dòng comment/docstring (chỉ bẻ code), nên Flake8 dùng chung ngưỡng 88 với Black chắc chắn luôn xung đột với comment dài. Đã nâng riêng `max-line-length` của Flake8 lên **160** (không đổi Black — Black vẫn 88 mặc định) ở 3 nơi: `ci.yml`, `.pre-commit-config.yaml`, và file mới `backend/setup.cfg` (cho dev chạy `flake8` tay không cần nhớ cờ).
    - **Lỗi phát sinh #5 — vài F401/F841 THẬT** (không phải noqa sai): `api/upload.py` import `HTTPException` không dùng; `tests/test_sanity.py` import `pytest` không dùng (test không cần fixture/decorator gì của pytest); `services/fleet_service.py` có `settings = get_settings(db)` ở `get_smart_alerts()` nhưng không hề dùng `settings` ở đâu trong hàm (dead code từ trước, không phải do các thay đổi gần đây) — đã xoá cả 3.
    - **Lỗi phát sinh #6 — noqa "mồ côi" do Isort tự tách dòng import**: Isort (dù đã có `--profile black`) đôi khi tách 1 dòng `from x import A, B` có sẵn `# noqa` thành 2 dòng riêng (`from x import A  # noqa` và `from x import B` — dòng B KHÔNG có noqa) — xảy ra ở `main.py` (`from models.user import Base as UserBase, User`) và `scripts/seed_fleet_data.py` (`from services.fleet_parser import ...`). **Bài học: sau khi chạy Isort, PHẢI chạy lại Flake8 để bắt các dòng noqa bị "mồ côi" kiểu này — không thể tin Isort tự bảo toàn comment 100% khi nó tách dòng.** Cũng đổi 4 import model chỉ-để-đăng-ký-với-SQLAlchemy (`Vehicle`, `MaintenanceRecord`, `VehicleMaintenanceStatus`, `AppSettings`) từ `from models.x import X` sang `import models.x` (import cả module) trong `main.py` — tránh việc Black bẻ dòng `from x import (Name,)` thành 3 dòng khi có noqa dài, vì noqa đặt ở dòng đóng `)` không còn tác dụng cho lỗi báo ở dòng mở `from x import (`.
    - **Lỗi phát sinh #7 — Bandit B104** (medium): cảnh báo bind `0.0.0.0` trong `uvicorn.run(...)` ở `main.py` — đây là CHỦ Ý (cần truy cập từ LAN, không chỉ localhost, theo đúng thiết kế đã làm ngày 2026-08-04/05) — thêm `# nosec B104` kèm comment giải thích, không tắt Bandit hay bỏ qua toàn file.
    - **Kết quả cuối**: cả 2 job CI (`Backend Code Review` + `Frontend Code Review`) chạy **XANH** trên GitHub thật (không chỉ chạy local) — đã tự xác nhận qua GitHub API (`/actions/runs`, `/actions/runs/{id}/jobs`), không chỉ tin vào kết quả chạy tay. Đã tự kiểm tra thêm: backend khởi động lại + gọi API `/` thật trả 200 sau toàn bộ các sửa đổi format, đảm bảo không có thay đổi nào làm hỏng hành vi thật.
  - **Cập nhật (2026-08-12) — đã xử lý nợ kỹ thuật SECRET_KEY (trước đó chỉ cảnh báo console, chưa chặn cứng):**
    - Thêm `python-dotenv==1.0.1` vào `requirements.txt` + cài đặt. `main.py`: gọi `load_dotenv(Path(__file__).resolve().parent.parent / ".env")` ở **dòng ĐẦU TIÊN, trước mọi import khác** — bắt buộc vì `core/security.py` đọc `SECRET_KEY` bằng `os.getenv()` ngay lúc import module (không lazy), nên nếu `load_dotenv()` chạy sau `from api.auth import ...` thì đã quá muộn. Đường dẫn `.env` chỉ định rõ tới gốc dự án (`dashboard/md/.env`, cùng cấp `.env.example`) — KHÔNG dùng `load_dotenv()` mặc định vì mặc định chỉ tìm ở working directory (`backend/`), không có file ở đó. Các import phía sau `load_dotenv()` phải thêm `# noqa: E402` (Flake8 báo lỗi "import không ở đầu file" — chấp nhận được vì đây là trường hợp bắt buộc phải load env trước, không phải code luộm thuộm).
    - Tạo file `.env` THẬT ở gốc dự án (`dashboard/md/.env`, đã có `.env`/`.env.*` trong `.gitignore` từ trước nên an toàn) — `SECRET_KEY` sinh bằng `secrets.token_urlsafe(64)` (khoá ngẫu nhiên 64 byte, không phải giá trị hardcode trong code nữa). `DATABASE_URL`/`VITE_API_BASE_URL` giữ nguyên dạng placeholder như `.env.example` vì 2 biến này CHƯA được code thật nào đọc tới (`core/database.py` hardcode SQLite, frontend gọi API bằng đường dẫn tương đối) — ghi rõ trong comment để người sau không hiểu lầm là đã cấu hình xong Postgres.
    - Đã tự kiểm chứng: khởi động lại backend, dòng cảnh báo `[security] CẢNH BÁO: đang dùng SECRET_KEY mặc định...` KHÔNG còn xuất hiện nữa (chứng minh key thật đã được nạp) — login + gọi API `/fleet/stats` bằng token mới vẫn trả đúng 200/207 xe, xác nhận JWT ký bằng key mới hoạt động bình thường.
    - **Lưu ý phụ phát sinh**: đổi `SECRET_KEY` làm mọi JWT token ký bằng key CŨ lập tức vô hiệu (đây là hành vi đúng/mong đợi, không phải bug) — tài khoản admin phải đăng nhập lại; đã tiện thể đặt lại mật khẩu admin 1 lần nữa vì mật khẩu cũ ghi trong hội thoại trước đó không khớp (nghi do đã bị ghi đè ở 1 lần seed/restore trước mà không kịp cập nhật lại thông báo cho người dùng) — mật khẩu admin hiện tại: `9LhdOTmE-h4A` (vẫn `must_change_password=true`, bắt đổi ngay lần đăng nhập đầu).

## 2. Quyết định Kiến trúc & Công nghệ (Key Decisions)
- Áp dụng cấu trúc thư mục quy tắc phân tách như hình mẫu: chia nhỏ thành `core_principles`, `design`, `workflow`, `coding_style`, `tech_task`.
- Ưu tiên tài liệu hóa rõ ràng trước khi đi vào code chi tiết.

## 3. Lỗi từng gặp & Bài học rút ra (Learnings & Gotchas)
- *(Mục này sẽ được cập nhật liên tục khi quá trình code phát sinh các lỗi khó nhằn hoặc bug tốn nhiều thời gian để debug, nhằm tránh mắc lại lần 2).*
- Luôn kiểm tra kỹ các biến môi trường trước khi push/deploy.

## 4. Công việc sắp tới (Todo / Backlog)
- [x] Tạo khung tài liệu `md` chi tiết.
- [x] Cấu hình Pre-commit Hook (Linter, Formatter, Security Scanner, Commitlint).
- [x] Thiết lập CI/CD Pipeline bằng GitHub Actions.
- [x] Tạo AI Reviewer Agent (`agents/reviewer_agent.md`).
- [x] Tạo Pull Request Template chuẩn Enterprise.
- [ ] **[ĐANG LÀM]** Điền đầy đủ thông tin vào `CLAUDE.md` (Tên dự án, Tech Stack).
- [ ] Hoàn thiện Giai đoạn 1 ROADMAP: CRUD API, Layout, Hiển thị dữ liệu.
- [ ] Áp dụng bộ tài liệu vào phát triển các module thực tế.
