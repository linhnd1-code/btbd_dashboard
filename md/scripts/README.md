# SCRIPTS

Thư mục này chứa các đoạn mã tự động hóa để hỗ trợ Developer.

## Ví dụ:
- `seed_data.js`: Chạy để tạo nhanh dữ liệu mẫu vào Database (User ảo, sản phẩm ảo...).
- `deploy.sh`: Script gõ 1 lệnh là tự động build và đẩy lên server.
- `cleanup.sh`: Script xóa các file log thừa, cache.
